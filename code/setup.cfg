[metadata]
name = capstone

[options]
packages = find:
package_dir = = .
include_package_data = true
python_requires = >= 3.6

[options.packages.find]
where = .

[tool:pytest]
testpaths = ./tests
filterwarnings =
    error
