apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
    role: green
    env: production
spec:
  type: LoadBalancer
  selector:
    app: nginx
    role: green
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
