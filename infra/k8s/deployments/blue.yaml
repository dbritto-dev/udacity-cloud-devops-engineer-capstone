apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-blue
  selector:
    matchLabels:
      app: nginx
      role: blue
spec:
  replicas: 4
  template:
    metadata:
      labels:
        app: nginx
        role: blue
    spec:
      containers:
        - name: nginx
          image: minorpatch/capstone-nginx:blue
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
